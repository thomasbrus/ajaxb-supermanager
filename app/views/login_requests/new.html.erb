<%- content_for(:title, "Login") %>

<h2>Inloggen op uw account</h2>

<p>Om in te loggen op uw account heeft u een bevestigingslink nodig. Vul het e-mailadres in waarmee u op de site geregistreerd bent en druk vervolgens op de knop.</p>

<%= form_for @login_request do |f| %>
  <% if @login_request.errors.any? %>
    <div id="error_explanation">
      <ul>
      <% @login_request.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field flex items-center">
    <%= label_tag :email, t('activerecord.attributes.contestant.email') %>
    <%= text_field_tag :email, @email, style: "padding: 10px; border-radius: 3px; border: 1px #ccc solid; width: 300px;" %>
  </div>
  <div class="action">
    <%= f.button type: 'submit', class: 'cta' do %>
<span>Verzend link</span>
    <% end %>
  </div>
<% end %>
